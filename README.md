Takes a file named card_codes with a list of arkhamdb card codes separated by whitespace and spits out the results in the results/ directory.
Can parse all cards except for investigator (so far)

This project was originally built for Lua and included functions for card actions.
Project is being rewritten in roblox-ts, so a rewrite is underway.